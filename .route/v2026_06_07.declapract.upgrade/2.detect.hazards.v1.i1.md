# 2.detect.hazards — iteration 1

## hazards detected

### test hazards

#### 1. relative imports → absolute imports (7 files)

**what changed:**
- `../index` → `@src/index` in integration and acceptance test files

**why it may break:**
- if `@src/` path mapping is not configured correctly in jest, tests will fail with module resolution errors

**verify:**
```sh
rhx git.repo.test --what integration --mode apply
```

#### 2. jest config: slowtest reporter added

**what changed:**
- added `['test-fns/slowtest.reporter.jest', { slow: '10s', output: '.log/slowtest/integration.report.json' }]` to reporters

**why it may break:**
- if `.log/` directory doesn't exist or isn't writable, reporter may fail
- if test-fns version mismatch, reporter may not export correctly

**verify:**
```sh
rhx git.repo.test --what integration --mode apply
```

#### 3. jest env: node: prefix imports

**what changed:**
- `child_process` → `node:child_process`
- `fs` → `node:fs`
- `path` → `node:path`
- `util` → `node:util`

**why it may break:**
- older node versions (<16) don't support `node:` prefix
- should be fine with node 22

**verify:**
```sh
rhx git.repo.test --what types
```

#### 4. jest env: @jest/globals import added

**what changed:**
- added `import { jest } from '@jest/globals'`
- removed `// eslint-disable-next-line no-undef` comment

**why it may break:**
- if @jest/globals version mismatches jest version, may cause type errors

**verify:**
```sh
rhx git.repo.test --what types
```

#### 5. jest env: environment variable renames

**what changed:**
- `STAGE` → `CONFIG`
- `I_KNOW_WHAT_IM_DOING` → `I_KNOW_THE_RISKS`

**why it may break:**
- if any workflows or commands use old variable names, env checks will fail

**verify:**
- check `.github/workflows/*.yml` for old var names
- run integration tests

### config hazards

#### 6. tsconfig: module system change

**what changed:**
- `"module": "commonjs"` → `"module": "node16"`
- `"moduleResolution": "node"` → `"moduleResolution": "node16"`

**why it may break:**
- node16 module resolution is stricter
- may require file extensions in imports
- may affect how esm vs cjs modules are resolved

**verify:**
```sh
rhx git.repo.test --what types
npm run build
```

#### 7. error package import change

**what changed:**
- `@ehmpathy/error-fns` → `helpful-errors` in assertAwsCredentialsPresent.ts

**why it may break:**
- if helpful-errors doesn't export BadRequestError with same api, will fail
- both packages exist in deps, so should be compatible

**verify:**
```sh
rhx git.repo.test --what types
```

### cicd hazards

#### 8. test workflow shards (added on re-scan)

**what changed:**
- `test-integration` job renamed to `test-shards-integration`
- `test-acceptance` job renamed to `test-shards-acceptance`
- added `enshard` job for shard matrix setup
- added `test-shards-omit` placeholder job
- tests now run in parallel shards

**why it may break:**
- if shards config files don't exist (`jest.integration.shards.jsonc`, `jest.acceptance.shards.jsonc`), enshard job fails
- if matrix outputs are empty, no tests run

**verify:**
- check if shard config files exist
- run ci pipeline

#### 9. github provision changes (added on re-scan)

**what changed:**
- added `suite / enshard` to required status checks in branch protection
- added `DeclaredGithubEnvironment` for production deployments
- added `production-on-main` and `production-on-else` environments

**why it may break:**
- if `enshard` job fails, PRs cannot be merged
- if `releasers` team doesn't exist, environment provision fails

**verify:**
- run `npm run provision` after merge

#### 10. test-types now builds first (added on re-scan)

**what changed:**
- test-types job now runs `npm run build` before `npm run test:types`

**why it may break:**
- if build fails, test:types won't run
- increases test-types job duration

**verify:**
```sh
npm run build
rhx git.repo.test --what types
```

#### 11. new dpdm cycle detection

**what changed:**
- added `test:lint:cycles` command with dpdm
- added new `.dpdmrc.yaml` config

**why it may break:**
- if cycles exist in codebase, lint will fail
- dpdm command requires yq to parse yaml

**verify:**
```sh
rhx git.repo.test --what lint
```

#### 12. test command changes

**what changed:**
- all test commands now use `set -eu` prefix
- variable handling changed: `${CI:-}`, `${THOROUGH:-}`, `${RESNAP:-}`

**why it may break:**
- stricter error handling may surface previously silent failures

**verify:**
```sh
rhx git.repo.test --what unit --mode apply
rhx git.repo.test --what integration --mode apply
```

### path hazards

#### 13. use.apikeys.sh deleted

**what changed:**
- `.agent/repo=.this/role=any/skills/use.apikeys.sh` deleted

**why it may break:**
- if any commands source this file directly, they will fail
- replaced by keyrack source in jest.*.env.ts

**verify:**
- check if any workflows reference this file

## summary

| category | count | severity |
|----------|-------|----------|
| test | 5 | medium |
| config | 2 | high |
| cicd | 5 | medium-high |
| path | 1 | low |

total: 13 hazards

## next steps

1. run type check to verify tsconfig changes
2. run lint to verify dpdm cycle detection
3. run tests to verify jest changes
